"use client";import type{AIModel}from "../../../lib/ai/contracts";export default function ModelSelector({models,value,onChange,disabled}:{models:AIModel[];value:AIModel;onChange:(m:AIModel)=>void;disabled?:boolean}){return <label>Model <select aria-label="AI model" value={value} disabled={disabled} onChange={e=>onChange(e.target.value as AIModel)}>{models.map(m=><option key={m}>{m}</option>)}</select></label>}
