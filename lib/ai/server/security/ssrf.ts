import "server-only";import {isIP} from "node:net";export function assertAllowedUrl(input:string,p:{hosts:string[];pathPrefixes:string[]}){const u=new URL(input);if(u.protocol!=="https:"||u.username||u.password||isIP(u.hostname)||u.hostname==="localhost"||u.hostname.endsWith(".localhost")||!p.hosts.includes(u.hostname)||!p.pathPrefixes.some(x=>u.pathname.startsWith(x)))throw new Error("URL not allowed");return u;}
