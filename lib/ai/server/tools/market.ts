import {readOrUnavailable,type Reader} from "./result";type Args={symbol?:string;interval?:string};export function createMarketTools(readers:{price?:Reader<Args,unknown>;candles?:Reader<Args,unknown>;sentiment?:Reader<Args,unknown>}={}){return{price:(a:Args)=>readOrUnavailable("internal:okx:market",readers.price,a),candles:(a:Args)=>readOrUnavailable("internal:okx:candles",readers.candles,a),sentiment:(a:Args)=>readOrUnavailable("internal:okx:sentiment",readers.sentiment,a)};}
