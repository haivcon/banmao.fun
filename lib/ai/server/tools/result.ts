export type ToolResult<T>={status:"available";value:T;source:string;observedAt:string;blockNumber?:string}|{status:"unavailable";reason:string;source:string;observedAt:string};export type Reader<TArgs,T>=(args:TArgs)=>Promise<{value:T;source:string;blockNumber?:bigint}>;export async function readOrUnavailable<TArgs,T>(source:string,reader:Reader<TArgs,T>|undefined,args:TArgs):Promise<ToolResult<T>>{const observedAt=new Date().toISOString();if(!reader)return{status:"unavailable",reason:"No approved local read adapter is configured",source,observedAt};try{const r=await reader(args);return{status:"available",value:r.value,source:r.source,observedAt,blockNumber:r.blockNumber?.toString()};}catch{return{status:"unavailable",reason:"Approved data source is unavailable",source,observedAt};}}
