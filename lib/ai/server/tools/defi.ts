import {readOrUnavailable,type Reader} from "./result";type Args={chainId:number};export function createDefiTools(readers:{staking?:Reader<Args,unknown>;box?:Reader<Args,unknown>;burn?:Reader<Args,unknown>;airdrop?:Reader<Args,unknown>}={}){return{staking:(a:Args)=>readOrUnavailable("internal:defi:staking",readers.staking,a),box:(a:Args)=>readOrUnavailable("internal:defi:box",readers.box,a),burn:(a:Args)=>readOrUnavailable("internal:defi:burn",readers.burn,a),airdrop:(a:Args)=>readOrUnavailable("internal:defi:airdrop",readers.airdrop,a)};}
