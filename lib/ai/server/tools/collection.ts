import {readOrUnavailable,type Reader} from "./result";type Args={id?:string;query?:string};export function createCollectionTools(readers:{metadata?:Reader<Args,unknown>;search?:Reader<Args,unknown>;caption?:Reader<Args,unknown>}={}){return{metadata:(a:Args)=>readOrUnavailable("internal:collection:metadata",readers.metadata,a),search:(a:Args)=>readOrUnavailable("internal:collection:search",readers.search,a),caption:(a:Args)=>readOrUnavailable("internal:collection:caption",readers.caption,a)};}
