# Banmao Fun - Web3D Updates

## v0.1.1 (Latest Updates)

### 🐋 3D Ecosystem Enhancements: The Swimming Whale
- **Model Integration**: Successfully integrated the `banmao-whale.glb` 3D model into the Web3D space.
- **Skinned Mesh Support**: Refactored the loading mechanism to use `@react-three/drei`'s `<Clone>` component, properly supporting the model's armature, skeleton, and original textures without mutation.
- **Animation System**: Replaced the complex Lissajous path with a stable **Elliptical Orbit** pattern. The whale now swims continuously in the upper-right quadrant (near the OKX logo) and is mathematically locked to remain in front of the camera (`z > 0`).
- **Dynamic Lighting**: Implemented a dedicated 3-point lighting rig (White Key light, Cyan Rim light, Warm Fill light) that travels alongside the whale to enhance visibility and preserve native textures without causing emissive artifacts.
- **Responsive Scaling**: Optimized the whale's scale and orbit radius distinctly for Desktop (`scale=15`) and Mobile (`scale=10`) viewports.

### 📱 Mobile UI/UX & Layout Fixes
- **HTML Scaling Engine (`useHtmlScale`)**: Resolved the "double-penalty" shrinking issue on mobile devices. Removed the excessive FOV tangent correction and raised the minimum clamp, boosting the base mobile scale from ~0.54 to ~0.76.
- **Token Info Panel (`TokenInfoPanel3D`)**: 
  - Expanded mobile container width to `500px`.
  - Removed excessive horizontal padding to allow content to naturally fill the 3D plane.
  - Significantly increased mobile typography (`logo-emoji`, `logo-text` to 32px, `description` to 18px) to guarantee readability without manual zooming.

---
*Generated by AI Agent during active development.*
